#include <stdbool.h>
#include <string.h>
#include <pthread.h>

//this c file for making as many threads as there are active chess games.

//the first func in a thread that manages a game 
//once two players are paired from the lobby manager thread
void* chessGameThreadStart(void*)
{
    
}